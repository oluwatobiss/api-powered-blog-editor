---
import UserPosts from "../components/UserPosts";
import Layout from "../layouts/Layout.astro";
const pageTitle = "Home | staffBlog";
---

<Layout pageTitle={pageTitle}>
  <UserPosts client:only="react" />
</Layout>
